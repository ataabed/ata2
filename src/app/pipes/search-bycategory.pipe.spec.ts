import { SearchBycategoryPipe } from './search-bycategory.pipe';

describe('SearchBycategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchBycategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
